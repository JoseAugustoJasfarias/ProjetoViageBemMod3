@model IEnumerable<ViajeBem.Models.Destino>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeDestino)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pais)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataViagem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeDestino)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pais)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataViagem)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Destino_Cliente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Destino_Cliente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Destino_Cliente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
