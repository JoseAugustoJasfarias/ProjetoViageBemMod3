@model IEnumerable<ViajeBem.Models.Cliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TelefoneCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id_Destino_Cliente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TelefoneCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Destino_Cliente)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Cliente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Cliente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Cliente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
